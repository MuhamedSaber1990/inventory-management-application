<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Add Category</title>
    <link rel="stylesheet" href="/css/app.css" />
    <link rel="stylesheet" href="/css/form.css" />
  </head>
  <body class="auth-body">
    <div class="auth-wrapper" style="max-width: 600px">
      <div class="auth-card">
        <header class="auth-header">
          <h1>Add New Category</h1>
          <p>Create a new product category</p>
        </header>

        <% if (errorMessage) { %>
        <div class="auth-alert auth-alert-error">⚠️ <%= errorMessage %></div>
        <% } %>

        <form action="/categories/new" method="POST" class="auth-form">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

          <div class="form-group">
            <label for="name" class="form-label">Category Name *</label>
            <input
              type="text"
              id="name"
              name="name"
              class="form-input"
              placeholder="e.g., Electronics, Furniture"
              value="<%= old.name || '' %>"
              required
            />
          </div>

          <div class="form-group">
            <label for="description" class="form-label">Description</label>
            <textarea
              id="description"
              name="description"
              class="form-input"
              rows="4"
              placeholder="Optional category description..."
            >
<%= old.description || '' %></textarea
            >
          </div>

          <div class="form-actions">
            <button type="submit" class="auth-btn">Create Category</button>
            <a href="/categories" class="btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
